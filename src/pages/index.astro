---
import { ghostClient } from "../lib/ghost";
const posts = await ghostClient.posts
  .browse({
    limit: "all",
  })
  .catch((err) => {
    console.error(err);
  });
---

<html lang="en">
  <head>
    <title>Astro + Ghost 👻</title>
  </head>
  <body>
    {
      posts.map((post) => (
        <a
          class=" text-red-400 hover:text-blue-400 text-md"
          href={`/post/${post.slug}`}
        >
          <h1 class="mt-4 ml-4"> {post.title} </h1>
        </a>
      ))
    }
  </body>
</html>
